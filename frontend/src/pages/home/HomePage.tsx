import React from 'react';
import Header from "@/components/header/Header.tsx";

const HomePage = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default HomePage;
